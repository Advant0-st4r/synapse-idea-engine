# .env.example
# Clerk frontend API (publishable / frontend)
VITE_CLERK_FRONTEND_API=clerk.your-frontend-api.example

# Other environment variables for backend endpoints go here, but keep secrets only on server.
